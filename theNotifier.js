const errors = ["variable not declared", 
                "error in line 24",
                "expecting a number in line 15",
                "property is no defined" 
            ];

errors.forEach(item => console.log(`⚠️ ${item}`)) 